# Runs on every push to feat/* branches. Publish-only:
# 1. Builds all container images tagged with the feature name
#    (e.g. feat/ting-ui -> tag "ting-ui")
# 2. Packages Helm charts as 0.0.0-<feature>.<N> prerelease versions
#    (e.g. 0.0.0-ting-ui.3)
#
# Test/lint for feature branches lives in ci.yaml on pull_request events —
# every niu-*/feat PR runs them there. Pushing directly to feat/* without a
# PR skips tests by design; branch protection should require PR review.
name: Feature Build

on:
  push:
    branches: ["feat/**"]

permissions: read-all

env:
  REGISTRY: ghcr.io
  CHART_REPO: oci://ghcr.io/${{ github.repository_owner }}/charts

concurrency:
  group: feature-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Extract feature name from branch for tagging
  # ---------------------------------------------------------------------------
  meta:
    name: Extract Feature Tag
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
      chart_version: ${{ steps.tag.outputs.chart_version }}
      short_sha: ${{ steps.tag.outputs.short_sha }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Derive tag from branch name
        id: tag
        run: |
          # Strip "feat/" prefix -> use remainder as tag
          # e.g. feat/ting-ui -> ting-ui, feat/auth/oidc -> auth-oidc
          RAW="${GITHUB_REF_NAME#feat/}"
          # Replace slashes with dashes for valid Docker tag
          TAG="${RAW//\//-}"
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "Feature tag: ${TAG}"

          # Compute chart version: 0.0.0-<feature>.<N>
          # N = number of commits on this branch (gives monotonic build number)
          N=$(git rev-list --count HEAD)
          CHART_VERSION="0.0.0-${TAG}.${N}"
          echo "chart_version=${CHART_VERSION}" >> $GITHUB_OUTPUT
          echo "Chart version: ${CHART_VERSION}"

          echo "short_sha=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

  # ---------------------------------------------------------------------------
  # Containers — build multi-arch, publish feature tag + feature-<short-sha>.
  # Delegated to reusable workflow.
  # ---------------------------------------------------------------------------
  containers:
    name: Containers
    needs: meta
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/reusable-container-build.yaml
    with:
      containers: '["agent","skuld","openshell","devrunner","volundr-web","niuu-web","niuu"]'
      platforms: '["amd64","arm64"]'
      tag-prefix: ${{ needs.meta.outputs.tag }}
      manifest-tags: "${{ needs.meta.outputs.tag }} ${{ needs.meta.outputs.tag }}-${{ needs.meta.outputs.short_sha }}"
      build-args: |
        APP_VERSION=${{ needs.meta.outputs.tag }}-${{ github.sha }}
      labels: |
        org.opencontainers.image.title=niuu
        org.opencontainers.image.licenses=Apache-2.0
        org.opencontainers.image.vendor=Niuu Labs
        org.opencontainers.image.source=https://github.com/${{ github.repository }}
        dev.niuu.feature=${{ needs.meta.outputs.tag }}

  # ---------------------------------------------------------------------------
  # Helm charts — feature prerelease versions (0.0.0-<feature>.<N>)
  # ---------------------------------------------------------------------------
  build-charts:
    name: Build Helm Charts
    needs: meta
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1
        with:
          version: v3.14.0

      - name: Update chart versions
        run: |
          VERSION="${{ needs.meta.outputs.chart_version }}"
          TAG="${{ needs.meta.outputs.tag }}"

          for CHART in agent bifrost ravn skuld volundr ting niuu-shared niuu; do
            sed -i "s/^version:.*/version: $VERSION/" "charts/${CHART}/Chart.yaml"
            sed -i "s/^appVersion:.*/appVersion: \"$VERSION\"/" "charts/${CHART}/Chart.yaml"
          done

          # Point image tags to the feature tag so the chart deploys feature images
          for CHART in agent bifrost ravn skuld volundr ting; do
            if [ -f "charts/${CHART}/values.yaml" ]; then
              sed -i "s/^  tag:.*/  tag: \"${TAG}\"/" "charts/${CHART}/values.yaml" || true
            fi
          done

      - name: Package Helm charts
        run: |
          helm package charts/bifrost -d .helm-packages/
          helm package charts/agent -d .helm-packages/
          helm package charts/ravn -d .helm-packages/
          helm package charts/skuld -d .helm-packages/
          helm package charts/volundr -d .helm-packages/
          helm package charts/ting -d .helm-packages/
          helm package charts/niuu-shared -d .helm-packages/
          rm -f charts/niuu/Chart.lock
          helm dependency update charts/niuu
          helm package charts/niuu -d .helm-packages/

      - name: Log in to Container Registry
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Helm charts to OCI registry
        run: |
          helm push .helm-packages/agent-*.tgz ${{ env.CHART_REPO }}
          helm push .helm-packages/bifrost-*.tgz ${{ env.CHART_REPO }}
          helm push .helm-packages/ravn-*.tgz ${{ env.CHART_REPO }}
          helm push .helm-packages/skuld-*.tgz ${{ env.CHART_REPO }}
          helm push .helm-packages/volundr-*.tgz ${{ env.CHART_REPO }}
          helm push .helm-packages/ting-*.tgz ${{ env.CHART_REPO }}
          helm push .helm-packages/niuu-shared-*.tgz ${{ env.CHART_REPO }}
          helm push .helm-packages/niuu-[0-9]*.tgz ${{ env.CHART_REPO }}
