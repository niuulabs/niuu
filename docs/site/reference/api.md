# API Reference

Use the generated OpenAPI document for exact REST shapes.

The docs build extracts OpenAPI into:

```text
openapi.json
```

When the local stack is running, interactive FastAPI docs are also available under each mounted service's `/docs` path when that service exposes them.

## Stability

APIs are under active development. Prefer generated clients or direct OpenAPI inspection before depending on a route shape in automation.

<a href="../openapi.json">Download the generated OpenAPI document</a>
