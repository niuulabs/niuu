# niuu CLI reference

`niuu` drives the platform itself: authentication, server contexts, the
local stack, coding sessions, runs, and sagas.

Start with the worked examples below; the full command tree follows.
For the agent runtime CLI see [ravn](cli-ravn.md).

<!-- Generated by scripts/generate_cli_docs.py — do not edit by hand. -->

## Common tasks

### Run the local stack

The scripts are the short path for day-to-day work. Use the CLI directly when you are debugging the platform host itself.

```bash
./start-dev                 # full local stack on :8080, mini mode
./stop-dev

niuu platform up            # same thing, driven directly
niuu platform status        # health of every registered service
niuu platform down
```

### Choose what starts

Services resolve from plugin defaults, then config, then these flags. `--all` overrides everything.

```bash
niuu platform up --all                     # every registered service
niuu platform up --no-web                  # backend only, no web UI
niuu platform up --no-mimir --no-ting      # skip specific services
niuu platform up --skip-preflight          # bypass the host checks
```

### First-time setup

```bash
niuu platform init          # interactive setup wizard
niuu config show            # what the CLI is currently using
niuu config set server.port 8090
```

### Authenticate and switch servers

A context is a named server the CLI talks to, so you can move between a local stack and a shared one.

```bash
niuu login
niuu whoami

niuu context list
niuu context add staging https://niuu.example.com
niuu context use staging
```

### Work with coding sessions

```bash
niuu sessions list
niuu sessions create my-feature
niuu sessions stop <session-id>
niuu sessions list --json          # machine-readable
```

### Approve and follow autonomous work

Runs are individual executions; sagas are the longer campaigns that dispatch them.

```bash
niuu runs active
niuu runs approve <run-id>
niuu runs reject <run-id>

niuu sagas list
niuu sagas create nightly-cleanup
niuu sagas dispatch <saga-id>
```

### Watch it all

```bash
niuu tui                    # interactive terminal UI
niuu ravn list              # active agent sessions
niuu ravn status            # Ravn platform status
```

## Every command

### `niuu`

The Niuu platform CLI.

```bash
niuu [OPTIONS] COMMAND [ARGS]...
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--version`, `-V` | flag |  | Print version and exit. |
| `--home` | TEXT |  | Config directory (default: ~/.niuu, env NIUU_HOME). Env: `NIUU_HOME` |
| `--config` | TEXT |  | Config file path (default: ~/.niuu/config.yaml). |
| `--install-completion` | flag |  | Install completion for the current shell. |
| `--show-completion` | flag |  | Show completion for the current shell, to copy it or customize the installation. |

| Subcommand | Description |
| --- | --- |
| [`config`](#niuu-config) | Show or update configuration. |
| [`context`](#niuu-context) | Manage server contexts. |
| [`login`](#niuu-login) | Authenticate with the Niuu platform. |
| [`logout`](#niuu-logout) | Clear stored credentials. |
| [`platform`](#niuu-platform) | Manage the platform (up, down, status, init). |
| [`ravn`](#niuu-ravn) | Manage Ravn AI agent sessions. |
| [`runs`](#niuu-runs) | Manage runs. |
| [`sagas`](#niuu-sagas) | Manage sagas. |
| [`sessions`](#niuu-sessions) | Manage coding sessions. |
| [`tui`](#niuu-tui) | Launch the interactive TUI. |
| [`version`](#niuu-version) | Print the niuu CLI version. |
| [`whoami`](#niuu-whoami) | Show the currently authenticated user. |

#### `niuu config`

Show or update configuration.

```bash
niuu config [OPTIONS] COMMAND [ARGS]...
```

| Subcommand | Description |
| --- | --- |
| [`set`](#niuu-config-set) | Set a configuration value. |
| [`show`](#niuu-config-show) | Show current CLI configuration. |

##### `niuu config set`

Set a configuration value.

```bash
niuu config set [OPTIONS] KEY VALUE
```

| Argument | Required | Description |
| --- | --- | --- |
| `KEY` | yes | Config key (e.g. server.port) |
| `VALUE` | yes | New value |

##### `niuu config show`

Show current CLI configuration.

```bash
niuu config show [OPTIONS]
```

#### `niuu context`

Manage server contexts.

```bash
niuu context [OPTIONS] COMMAND [ARGS]...
```

| Subcommand | Description |
| --- | --- |
| [`add`](#niuu-context-add) | Add a new server context. |
| [`delete`](#niuu-context-delete) | Remove a context. |
| [`list`](#niuu-context-list) | List all configured contexts. |
| [`use`](#niuu-context-use) | Switch to a context. |

##### `niuu context add`

Add a new server context.

```bash
niuu context add [OPTIONS] NAME URL
```

| Argument | Required | Description |
| --- | --- | --- |
| `NAME` | yes | Context name |
| `URL` | yes | Server URL |

##### `niuu context delete`

Remove a context.

```bash
niuu context delete [OPTIONS] NAME
```

| Argument | Required | Description |
| --- | --- | --- |
| `NAME` | yes | Context name to remove |

##### `niuu context list`

List all configured contexts.

```bash
niuu context list [OPTIONS]
```

##### `niuu context use`

Switch to a context.

```bash
niuu context use [OPTIONS] NAME
```

| Argument | Required | Description |
| --- | --- | --- |
| `NAME` | yes | Context name to activate |

#### `niuu login`

Authenticate with the Niuu platform.

```bash
niuu login [OPTIONS]
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--issuer`, `-i` | TEXT |  | OIDC issuer URL. Env: `NIUU_OIDC_ISSUER` |
| `--client-id` | TEXT | `niuu-cli` | OIDC client ID. Env: `NIUU_OIDC_CLIENT_ID` |

#### `niuu logout`

Clear stored credentials.

```bash
niuu logout [OPTIONS]
```

#### `niuu platform`

Manage the platform (up, down, status, init).

```bash
niuu platform [OPTIONS] COMMAND [ARGS]...
```

| Subcommand | Description |
| --- | --- |
| [`down`](#niuu-platform-down) | Stop all running services. |
| [`init`](#niuu-platform-init) | Run the first-time setup wizard. |
| [`inventory`](#niuu-platform-inventory) | Show or export the route domains mounted by the niuu host. |
| [`status`](#niuu-platform-status) | Show health of all registered services. |
| [`up`](#niuu-platform-up) | Start platform services (use --all to start everything). |

##### `niuu platform down`

Stop all running services.

```bash
niuu platform down [OPTIONS]
```

##### `niuu platform init`

Run the first-time setup wizard.

```bash
niuu platform init [OPTIONS]
```

##### `niuu platform inventory`

Show or export the route domains mounted by the niuu host.

```bash
niuu platform inventory [OPTIONS]
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--host-profile` | TEXT | `full` | Host profile used to resolve mounted route domains. |
| `--mounts` | TEXT |  | Comma-separated route domains to inventory instead of the profile default. |
| `--json` | flag |  | Print route inventory as JSON. |
| `--out` | TEXT |  | Optional file path to write the JSON route inventory report. |

##### `niuu platform status`

Show health of all registered services.

```bash
niuu platform status [OPTIONS]
```

##### `niuu platform up`

Start platform services (use --all to start everything).

```bash
niuu platform up [OPTIONS]
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--skip-preflight`, `--no-skip-preflight` | flag |  | Start without running the host preflight checks. |
| `--all`, `--no-all` | flag |  | Start every registered service, ignoring per-service defaults. |
| `--no-web`, `--no-no-web` | flag |  | Start the backend services without serving the web UI. |
| `--host-profile` | TEXT | `full` | Host profile that decides which route domains are mounted. |
| `--mounts` | TEXT |  | Comma-separated route domains to mount instead of the profile default. |
| `--workspaces-dir` | TEXT |  | Directory for session workspaces. Mini mode only. |
| `--audit`, `--no-audit` | flag |  | Force the audit service on or off — Audit log query service. |
| `--bifrost`, `--no-bifrost` | flag |  | Force the bifrost service on or off — Anthropic-compatible LLM proxy. |
| `--credentials`, `--no-credentials` | flag |  | Force the credentials service on or off — Credential, secret, and MCP metadata service. |
| `--features`, `--no-features` | flag |  | Force the features service on or off — Feature catalog and preference service. |
| `--guild`, `--no-guild` | flag |  | Force the guild service on or off — Shared instance registry and aggregate service. |
| `--identity`, `--no-identity` | flag |  | Force the identity service on or off — Identity, tenancy, and token service. |
| `--integrations`, `--no-integrations` | flag |  | Force the integrations service on or off — Integration connection and OAuth service. |
| `--mimir`, `--no-mimir` | flag |  | Force the mimir service on or off — Knowledge service. |
| `--niuu`, `--no-niuu` | flag |  | Force the niuu service on or off — Shared platform services. |
| `--observatory`, `--no-observatory` | flag |  | Force the observatory service on or off — Observatory registry and live streams. |
| `--personas`, `--no-personas` | flag |  | Force the personas service on or off — Canonical persona registry service. |
| `--ravn`, `--no-ravn` | flag |  | Force the ravn service on or off — Agent runtime and session management. |
| `--ting`, `--no-ting` | flag |  | Force the ting service on or off — Autonomous saga coordinator. |
| `--tracker`, `--no-tracker` | flag |  | Force the tracker service on or off — Tracker issue, status, and repo mapping service. |
| `--volundr`, `--no-volundr` | flag |  | Force the volundr service on or off — AI-native development platform. |

#### `niuu ravn`

Manage Ravn AI agent sessions.

```bash
niuu ravn [OPTIONS] COMMAND [ARGS]...
```

| Subcommand | Description |
| --- | --- |
| [`list`](#niuu-ravn-list) | List active agent sessions. |
| [`status`](#niuu-ravn-status) | Show Ravn platform status. |
| [`stop`](#niuu-ravn-stop) | Stop a running agent session. |

##### `niuu ravn list`

List active agent sessions.

```bash
niuu ravn list [OPTIONS]
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu ravn status`

Show Ravn platform status.

```bash
niuu ravn status [OPTIONS]
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu ravn stop`

Stop a running agent session.

```bash
niuu ravn stop [OPTIONS] SESSION_ID
```

| Argument | Required | Description |
| --- | --- | --- |
| `SESSION_ID` | yes | Session ID |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

#### `niuu runs`

Manage runs.

```bash
niuu runs [OPTIONS] COMMAND [ARGS]...
```

| Subcommand | Description |
| --- | --- |
| [`active`](#niuu-runs-active) | List active runs. |
| [`approve`](#niuu-runs-approve) | Approve a pending run. |
| [`reject`](#niuu-runs-reject) | Reject a pending run. |
| [`retry`](#niuu-runs-retry) | Retry a failed run. |

##### `niuu runs active`

List active runs.

```bash
niuu runs active [OPTIONS]
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu runs approve`

Approve a pending run.

```bash
niuu runs approve [OPTIONS] RUN_ID
```

| Argument | Required | Description |
| --- | --- | --- |
| `RUN_ID` | yes | Run ID |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu runs reject`

Reject a pending run.

```bash
niuu runs reject [OPTIONS] RUN_ID
```

| Argument | Required | Description |
| --- | --- | --- |
| `RUN_ID` | yes | Run ID |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu runs retry`

Retry a failed run.

```bash
niuu runs retry [OPTIONS] RUN_ID
```

| Argument | Required | Description |
| --- | --- | --- |
| `RUN_ID` | yes | Run ID |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

#### `niuu sagas`

Manage sagas.

```bash
niuu sagas [OPTIONS] COMMAND [ARGS]...
```

| Subcommand | Description |
| --- | --- |
| [`create`](#niuu-sagas-create) | Create a new saga. |
| [`dispatch`](#niuu-sagas-dispatch) | Dispatch a saga for execution. |
| [`list`](#niuu-sagas-list) | List active sagas. |

##### `niuu sagas create`

Create a new saga.

```bash
niuu sagas create [OPTIONS] NAME
```

| Argument | Required | Description |
| --- | --- | --- |
| `NAME` | yes | Saga name |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu sagas dispatch`

Dispatch a saga for execution.

```bash
niuu sagas dispatch [OPTIONS] SAGA_ID
```

| Argument | Required | Description |
| --- | --- | --- |
| `SAGA_ID` | yes | Saga ID |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu sagas list`

List active sagas.

```bash
niuu sagas list [OPTIONS]
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

#### `niuu sessions`

Manage coding sessions.

```bash
niuu sessions [OPTIONS] COMMAND [ARGS]...
```

| Subcommand | Description |
| --- | --- |
| [`create`](#niuu-sessions-create) | Create a new session. |
| [`delete`](#niuu-sessions-delete) | Delete a session. |
| [`list`](#niuu-sessions-list) | List active sessions. |
| [`stop`](#niuu-sessions-stop) | Stop a running session. |

##### `niuu sessions create`

Create a new session.

```bash
niuu sessions create [OPTIONS] NAME
```

| Argument | Required | Description |
| --- | --- | --- |
| `NAME` | yes | Session name |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu sessions delete`

Delete a session.

```bash
niuu sessions delete [OPTIONS] SESSION_ID
```

| Argument | Required | Description |
| --- | --- | --- |
| `SESSION_ID` | yes | Session ID |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu sessions list`

List active sessions.

```bash
niuu sessions list [OPTIONS]
```

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

##### `niuu sessions stop`

Stop a running session.

```bash
niuu sessions stop [OPTIONS] SESSION_ID
```

| Argument | Required | Description |
| --- | --- | --- |
| `SESSION_ID` | yes | Session ID |

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--json` | flag |  | Output raw JSON. |

#### `niuu tui`

Launch the interactive TUI.

```bash
niuu tui [OPTIONS]
```

#### `niuu version`

Print the niuu CLI version.

```bash
niuu version [OPTIONS]
```

#### `niuu whoami`

Show the currently authenticated user.

```bash
niuu whoami [OPTIONS]
```
