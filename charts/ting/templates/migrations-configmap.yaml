{{- if .Values.migrations.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ting.fullname" . }}-migrations
  labels:
    {{- include "ting.labels" . | nindent 4 }}
data:
  000001_initial_schema.down.sql: |
    -- Rollback initial schema for ting saga coordinator

    DROP TABLE IF EXISTS confidence_events;
    DROP TABLE IF EXISTS dispatcher_state;
    DROP TABLE IF EXISTS runs;
    DROP TABLE IF EXISTS phases;
    DROP TABLE IF EXISTS sagas;

  000001_initial_schema.up.sql: |
    -- Initial schema for ting saga coordinator

    CREATE TABLE IF NOT EXISTS sagas (
        id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        tracker_id    TEXT NOT NULL,
        tracker_type  TEXT NOT NULL,
        slug          TEXT NOT NULL UNIQUE,
        name          TEXT NOT NULL,
        repos         TEXT[] NOT NULL DEFAULT '{}',
        confidence    FLOAT DEFAULT 0,
        created_at    TIMESTAMPTZ DEFAULT NOW()
    );

    CREATE TABLE IF NOT EXISTS phases (
        id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        saga_id       UUID NOT NULL REFERENCES sagas(id),
        tracker_id    TEXT NOT NULL,
        number        INT NOT NULL,
        name          TEXT NOT NULL,
        status        TEXT NOT NULL DEFAULT 'GATED',
        confidence    FLOAT DEFAULT 0
    );

    CREATE INDEX IF NOT EXISTS idx_phases_saga_id ON phases(saga_id);

    CREATE TABLE IF NOT EXISTS runs (
        id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        phase_id      UUID NOT NULL REFERENCES phases(id),
        tracker_id    TEXT NOT NULL,
        name          TEXT NOT NULL,
        declared_files TEXT[],
        estimate_hours FLOAT,
        status        TEXT NOT NULL DEFAULT 'PENDING',
        confidence    FLOAT DEFAULT 0,
        session_id    TEXT,
        branch        TEXT,
        chronicle_summary TEXT,
        retry_count   INT DEFAULT 0,
        created_at    TIMESTAMPTZ DEFAULT NOW(),
        updated_at    TIMESTAMPTZ DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_runs_phase_id ON runs(phase_id);
    CREATE INDEX IF NOT EXISTS idx_runs_status ON runs(status);

    CREATE TABLE IF NOT EXISTS confidence_events (
        id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        run_id       UUID NOT NULL REFERENCES runs(id),
        event_type    TEXT NOT NULL,
        delta         FLOAT NOT NULL,
        score_after   FLOAT NOT NULL,
        created_at    TIMESTAMPTZ DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_confidence_events_run_id ON confidence_events(run_id);

    CREATE TABLE IF NOT EXISTS dispatcher_state (
        id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        running       BOOL DEFAULT TRUE,
        threshold     FLOAT DEFAULT 0.75,
        updated_at    TIMESTAMPTZ DEFAULT NOW()
    );

  000002_integration_connections.down.sql: |
    DROP TABLE IF EXISTS integration_connections;

  000002_integration_connections.up.sql: |
    -- Integration connections (shared table used by niuu adapters)

    CREATE TABLE IF NOT EXISTS integration_connections (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        user_id VARCHAR(255) NOT NULL,
        integration_type VARCHAR(50) NOT NULL,
        adapter VARCHAR(500) NOT NULL,
        credential_name VARCHAR(253) NOT NULL,
        config JSONB NOT NULL DEFAULT '{}',
        enabled BOOLEAN NOT NULL DEFAULT true,
        slug VARCHAR(100) NOT NULL DEFAULT '',
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_integration_connections_user
        ON integration_connections(user_id, integration_type);

  000003_saga_repos_array.down.sql: |
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS repo TEXT NOT NULL DEFAULT '';
    UPDATE sagas SET repo = repos[1] WHERE array_length(repos, 1) > 0;
    ALTER TABLE sagas DROP COLUMN IF EXISTS repos;
    ALTER TABLE sagas DROP COLUMN IF EXISTS name;
    ALTER TABLE sagas DROP COLUMN IF EXISTS status;

  000003_saga_repos_array.up.sql: |
    -- Fix sagas table: ensure repos array, name, and status columns exist.
    -- The initial schema may already have these columns; this migration is
    -- idempotent for both old and new initial schemas.

    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS name TEXT NOT NULL DEFAULT '';
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS repos TEXT[] NOT NULL DEFAULT '{}';
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'ACTIVE';

    -- Migrate existing data from singular repo column (only if it exists).
    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1 FROM information_schema.columns
            WHERE table_name = 'sagas' AND column_name = 'repo'
        ) THEN
            UPDATE sagas SET repos = ARRAY[repo] WHERE repo IS NOT NULL AND repo != '';
        END IF;
    END $$;

    ALTER TABLE sagas DROP COLUMN IF EXISTS repo;

  000004_owner_id_sagas.down.sql: |
    -- Revert owner_id on sagas, restore user_id on integration_connections

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1 FROM information_schema.columns
            WHERE table_name = 'integration_connections' AND column_name = 'owner_id'
        ) THEN
            ALTER TABLE integration_connections RENAME COLUMN owner_id TO user_id;
        END IF;
    END $$;

    DROP INDEX IF EXISTS idx_sagas_owner_id;
    ALTER TABLE sagas DROP COLUMN IF EXISTS owner_id;

  000004_owner_id_sagas.up.sql: |
    -- Add owner_id to sagas, rename user_id→owner_id on integration_connections

    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS owner_id TEXT NOT NULL DEFAULT 'default';
    CREATE INDEX IF NOT EXISTS idx_sagas_owner_id ON sagas(owner_id);

    -- Align with Volundr convention (sessions.owner_id, workspaces FK, etc.)
    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1 FROM information_schema.columns
            WHERE table_name = 'integration_connections' AND column_name = 'user_id'
        ) THEN
            ALTER TABLE integration_connections RENAME COLUMN user_id TO owner_id;
        END IF;
    END $$;

  000005_dispatcher_owner.down.sql: |
    -- Revert per-user dispatcher state

    DROP INDEX IF EXISTS idx_dispatcher_state_owner;
    ALTER TABLE dispatcher_state DROP COLUMN IF EXISTS max_concurrent_runs;
    ALTER TABLE dispatcher_state DROP COLUMN IF EXISTS owner_id;

  000005_dispatcher_owner.up.sql: |
    -- Per-user dispatcher state: owner_id + max_concurrent_runs

    ALTER TABLE dispatcher_state ADD COLUMN IF NOT EXISTS owner_id TEXT NOT NULL DEFAULT 'default';
    ALTER TABLE dispatcher_state ADD COLUMN IF NOT EXISTS max_concurrent_runs INT NOT NULL DEFAULT 3;
    CREATE UNIQUE INDEX IF NOT EXISTS idx_dispatcher_state_owner ON dispatcher_state(owner_id);

  000006_run_pr_fields.down.sql: |
    -- Revert PR tracking fields on runs

    DROP INDEX IF EXISTS idx_runs_session_id;
    ALTER TABLE runs DROP COLUMN IF EXISTS pr_id;
    ALTER TABLE runs DROP COLUMN IF EXISTS pr_url;

  000006_run_pr_fields.up.sql: |
    -- PR tracking fields on runs

    ALTER TABLE runs ADD COLUMN IF NOT EXISTS pr_url TEXT;
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS pr_id  TEXT;
    CREATE INDEX IF NOT EXISTS idx_runs_session_id ON runs(session_id)
        WHERE session_id IS NOT NULL;

  000007_notification_subscriptions.down.sql: |
    -- Revert notification subscriptions

    DROP TABLE IF EXISTS notification_subscriptions;

  000007_notification_subscriptions.up.sql: |
    -- Notification subscriptions table

    CREATE TABLE IF NOT EXISTS notification_subscriptions (
        id         UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
        owner_id   TEXT        NOT NULL,
        channel    TEXT        NOT NULL,
        config     JSONB       NOT NULL DEFAULT '{}',
        enabled    BOOLEAN     NOT NULL DEFAULT true,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_notification_subs_owner
        ON notification_subscriptions(owner_id);

  000008_personal_access_tokens.down.sql: |
    DROP TABLE IF EXISTS personal_access_tokens;

  000008_personal_access_tokens.up.sql: |
    -- Personal access tokens for Ting API callers (e.g. Telegram bot, ODIN/Móði)
    -- owner_id is the IDP sub claim from Envoy headers, treated as an opaque string.
    -- NOTE: Unlike Volundr's PAT table, there is no FK on owner_id because Ting does
    -- not own a users table. owner_id is validated at the application layer via the
    -- IDP subject claim propagated through Envoy.

    CREATE TABLE IF NOT EXISTS personal_access_tokens (
        id           UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
        owner_id     TEXT        NOT NULL,
        name         TEXT        NOT NULL,
        token_hash   TEXT        NOT NULL,
        created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        expires_at   TIMESTAMPTZ,
        last_used_at TIMESTAMPTZ
    );

    CREATE INDEX IF NOT EXISTS idx_pats_owner_id ON personal_access_tokens(owner_id);

  000009_pat_unique_owner_name.down.sql: |
    DROP INDEX IF EXISTS idx_pats_owner_name;

  000009_pat_unique_owner_name.up.sql: |
    -- Enforce one PAT name per owner
    CREATE UNIQUE INDEX IF NOT EXISTS idx_pats_owner_name ON personal_access_tokens(owner_id, name);

  000010_saga_base_branch.down.sql: |
    ALTER TABLE sagas DROP COLUMN IF EXISTS base_branch;

  000010_saga_base_branch.up.sql: |
    -- Add base_branch column to sagas
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS base_branch TEXT NOT NULL DEFAULT 'main';

  000011_runs_reason_column.down.sql: |
    ALTER TABLE runs DROP COLUMN IF EXISTS reason;

  000011_runs_reason_column.up.sql: |
    -- Add reason column to runs for reject context
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS reason TEXT;

  000012_run_description.down.sql: |
    ALTER TABLE runs DROP COLUMN IF EXISTS acceptance_criteria;
    ALTER TABLE runs DROP COLUMN IF EXISTS description;

  000012_run_description.up.sql: |
    -- Add description and acceptance_criteria columns to runs for NativeAdapter

    ALTER TABLE runs ADD COLUMN IF NOT EXISTS description TEXT NOT NULL DEFAULT '';
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS acceptance_criteria TEXT[] NOT NULL DEFAULT '{}';

  000013_runs_status_index.down.sql: |
    DROP INDEX IF EXISTS idx_runs_status;

  000013_runs_status_index.up.sql: |
    CREATE INDEX IF NOT EXISTS idx_runs_status ON runs (status);

  000014_run_pr_columns.down.sql: |
    DROP INDEX IF EXISTS idx_runs_session_id;
    ALTER TABLE runs DROP COLUMN IF EXISTS pr_id;
    ALTER TABLE runs DROP COLUMN IF EXISTS pr_url;

  000014_run_pr_columns.up.sql: |
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS pr_url TEXT;
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS pr_id TEXT;
    CREATE INDEX IF NOT EXISTS idx_runs_session_id ON runs(session_id) WHERE session_id IS NOT NULL;

  000015_session_messages.down.sql: |
    DROP TABLE IF EXISTS session_messages;

  000015_session_messages.up.sql: |
    CREATE TABLE IF NOT EXISTS session_messages (
        id          UUID PRIMARY KEY,
        run_id     UUID NOT NULL REFERENCES runs(id),
        session_id  TEXT NOT NULL,
        content     TEXT NOT NULL,
        sender      TEXT NOT NULL DEFAULT 'user',
        created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS idx_session_messages_run_id
        ON session_messages (run_id);

    CREATE INDEX IF NOT EXISTS idx_session_messages_session_id
        ON session_messages (session_id);

  000016_run_progress.down.sql: |
    DROP TABLE IF EXISTS run_session_messages;
    DROP TABLE IF EXISTS run_confidence_events;
    DROP TABLE IF EXISTS run_progress;

    -- Restore the dispatched_sessions table that migration 000016 up dropped.
    CREATE TABLE IF NOT EXISTS dispatched_sessions (
        id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        session_id        TEXT NOT NULL UNIQUE,
        owner_id          TEXT NOT NULL,
        saga_id           UUID NOT NULL,
        tracker_issue_id  TEXT NOT NULL,
        status            TEXT NOT NULL DEFAULT 'running',
        created_at        TIMESTAMPTZ DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_dispatched_sessions_status ON dispatched_sessions(status);
    CREATE INDEX IF NOT EXISTS idx_dispatched_sessions_owner  ON dispatched_sessions(owner_id);

  000016_run_progress.up.sql: |
    -- Unified run progress tracking for all tracker adapters.
    -- LinearAdapter stores progress alongside Linear API issue data.
    -- NativeAdapter uses the existing runs table (no change needed).

    DROP TABLE IF EXISTS dispatched_sessions;

    CREATE TABLE IF NOT EXISTS run_progress (
        tracker_id          TEXT PRIMARY KEY,
        run_id             UUID NOT NULL DEFAULT gen_random_uuid(),
        owner_id            TEXT,
        phase_tracker_id    TEXT,
        saga_tracker_id     TEXT,
        status              TEXT NOT NULL DEFAULT 'PENDING',
        session_id          TEXT,
        confidence          FLOAT DEFAULT 0,
        pr_url              TEXT,
        pr_id               TEXT,
        retry_count         INT DEFAULT 0,
        reason              TEXT,
        chronicle_summary   TEXT,
        updated_at          TIMESTAMPTZ DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_run_progress_status ON run_progress(status);
    CREATE INDEX IF NOT EXISTS idx_run_progress_session ON run_progress(session_id) WHERE session_id IS NOT NULL;
    CREATE INDEX IF NOT EXISTS idx_run_progress_run_id ON run_progress(run_id);
    CREATE INDEX IF NOT EXISTS idx_run_progress_phase ON run_progress(phase_tracker_id) WHERE phase_tracker_id IS NOT NULL;

    CREATE TABLE IF NOT EXISTS run_confidence_events (
        id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        run_id     UUID NOT NULL,
        tracker_id  TEXT NOT NULL,
        event_type  TEXT NOT NULL,
        delta       FLOAT NOT NULL,
        score_after FLOAT NOT NULL,
        created_at  TIMESTAMPTZ DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_run_confidence_events_tracker ON run_confidence_events(tracker_id);

    CREATE TABLE IF NOT EXISTS run_session_messages (
        id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        run_id     UUID NOT NULL,
        tracker_id  TEXT NOT NULL,
        session_id  TEXT NOT NULL,
        content     TEXT NOT NULL,
        sender      TEXT NOT NULL DEFAULT 'user',
        created_at  TIMESTAMPTZ DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS idx_run_session_messages_tracker ON run_session_messages(tracker_id);
    CREATE INDEX IF NOT EXISTS idx_run_session_messages_session ON run_session_messages(session_id);

  000017_run_depends_on.down.sql: |
    -- Remove depends_on column from run_progress and runs.

    ALTER TABLE run_progress DROP COLUMN IF EXISTS depends_on;
    ALTER TABLE runs DROP COLUMN IF EXISTS depends_on;

  000017_run_depends_on.up.sql: |
    -- Add depends_on column to run_progress for inter-run dependency tracking.
    -- Values are run names (not IDs) within the same saga.

    ALTER TABLE run_progress ADD COLUMN IF NOT EXISTS depends_on TEXT[] DEFAULT '{}';

    -- Also add to the runs table used by NativeAdapter.
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS depends_on TEXT[] DEFAULT '{}';

  000018_run_reviewer_session.down.sql: |
    DROP INDEX IF EXISTS idx_runs_reviewer_session;
    ALTER TABLE runs DROP COLUMN IF EXISTS review_round;
    ALTER TABLE runs DROP COLUMN IF EXISTS reviewer_session_id;
    ALTER TABLE run_progress DROP COLUMN IF EXISTS review_round;
    ALTER TABLE run_progress DROP COLUMN IF EXISTS reviewer_session_id;

  000018_run_reviewer_session.up.sql: |
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS reviewer_session_id TEXT;
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS review_round INTEGER NOT NULL DEFAULT 0;
    CREATE INDEX IF NOT EXISTS idx_runs_reviewer_session ON runs (reviewer_session_id) WHERE reviewer_session_id IS NOT NULL;

    ALTER TABLE run_progress ADD COLUMN IF NOT EXISTS reviewer_session_id TEXT;
    ALTER TABLE run_progress ADD COLUMN IF NOT EXISTS review_round INTEGER NOT NULL DEFAULT 0;

  000019_run_identifier_url.up.sql: |
    -- Add identifier and url columns to runs for tracker display.

    ALTER TABLE runs ADD COLUMN IF NOT EXISTS identifier TEXT NOT NULL DEFAULT '';
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS url TEXT NOT NULL DEFAULT '';

  000020_saga_feature_branch.down.sql: |
    ALTER TABLE sagas DROP COLUMN IF EXISTS feature_branch;

  000020_saga_feature_branch.up.sql: |
    -- Add feature_branch column to sagas (used by dispatch to set working branch)
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS feature_branch TEXT NOT NULL DEFAULT 'main';

  000021_dispatcher_auto_continue.down.sql: |
    ALTER TABLE dispatcher_state DROP COLUMN IF EXISTS auto_continue;

  000021_dispatcher_auto_continue.up.sql: |
    ALTER TABLE dispatcher_state ADD COLUMN IF NOT EXISTS auto_continue BOOLEAN NOT NULL DEFAULT FALSE;

  000022_run_structured_outcome.down.sql: |
    -- Rollback: remove structured_outcome and outcome_event_type from runs

    ALTER TABLE runs DROP COLUMN IF EXISTS structured_outcome;
    ALTER TABLE runs DROP COLUMN IF EXISTS outcome_event_type;

  000022_run_structured_outcome.up.sql: |
    -- Migration: add structured_outcome and outcome_event_type to runs
    -- Stores the parsed outcome block from a completed Ravn session.

    ALTER TABLE runs ADD COLUMN IF NOT EXISTS structured_outcome JSONB;
    ALTER TABLE runs ADD COLUMN IF NOT EXISTS outcome_event_type TEXT;

  000023_workflows.down.sql: |
    DROP INDEX IF EXISTS idx_workflows_name;
    DROP INDEX IF EXISTS idx_workflows_owner_updated_at;
    DROP INDEX IF EXISTS idx_workflows_scope_updated_at;
    DROP TABLE IF EXISTS workflows;

  000023_workflows.up.sql: |
    -- Persisted workflow catalog for Ting workflow definitions.

    CREATE TABLE IF NOT EXISTS workflows (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        name TEXT NOT NULL,
        description TEXT NOT NULL DEFAULT '',
        version TEXT NOT NULL DEFAULT 'draft',
        scope TEXT NOT NULL DEFAULT 'user',
        owner_id TEXT,
        definition_yaml TEXT,
        graph_json JSONB NOT NULL DEFAULT '{"nodes": [], "edges": []}'::jsonb,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        CONSTRAINT workflows_scope_check CHECK (scope IN ('system', 'user')),
        CONSTRAINT workflows_owner_check CHECK (
            (scope = 'system' AND owner_id IS NULL)
            OR (scope = 'user' AND owner_id IS NOT NULL)
        )
    );

    CREATE INDEX IF NOT EXISTS idx_workflows_scope_updated_at
        ON workflows(scope, updated_at DESC);
    CREATE INDEX IF NOT EXISTS idx_workflows_owner_updated_at
        ON workflows(owner_id, updated_at DESC)
        WHERE owner_id IS NOT NULL;
    CREATE INDEX IF NOT EXISTS idx_workflows_name
        ON workflows(name);

  000024_saga_workflows.down.sql: |
    DROP INDEX IF EXISTS idx_sagas_workflow_id;
    ALTER TABLE sagas DROP COLUMN IF EXISTS workflow_snapshot;
    ALTER TABLE sagas DROP COLUMN IF EXISTS workflow_version;
    ALTER TABLE sagas DROP COLUMN IF EXISTS workflow_id;

  000024_saga_workflows.up.sql: |
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS workflow_id UUID;
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS workflow_version TEXT;
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS workflow_snapshot JSONB;

    CREATE INDEX IF NOT EXISTS idx_sagas_workflow_id
        ON sagas(workflow_id)
        WHERE workflow_id IS NOT NULL;



  000025_legacy_schema_compat.down.sql: |
    -- Best-effort rollback for the legacy Ting schema compatibility rename.

    ALTER INDEX IF EXISTS idx_run_session_messages_session RENAME TO idx_raid_session_messages_session;
    ALTER INDEX IF EXISTS idx_run_session_messages_tracker RENAME TO idx_raid_session_messages_tracker;
    ALTER INDEX IF EXISTS idx_run_confidence_events_tracker RENAME TO idx_raid_confidence_events_tracker;
    ALTER INDEX IF EXISTS idx_run_progress_phase RENAME TO idx_raid_progress_phase;
    ALTER INDEX IF EXISTS idx_run_progress_run_id RENAME TO idx_raid_progress_raid_id;
    ALTER INDEX IF EXISTS idx_run_progress_session RENAME TO idx_raid_progress_session;
    ALTER INDEX IF EXISTS idx_run_progress_status RENAME TO idx_raid_progress_status;
    ALTER INDEX IF EXISTS idx_session_messages_run_id RENAME TO idx_session_messages_raid_id;
    ALTER INDEX IF EXISTS idx_confidence_events_run_id RENAME TO idx_confidence_events_raid_id;
    ALTER INDEX IF EXISTS idx_runs_status RENAME TO idx_raids_status;
    ALTER INDEX IF EXISTS idx_runs_phase_id RENAME TO idx_raids_phase_id;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'dispatcher_state' AND column_name = 'max_concurrent_runs'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'dispatcher_state' AND column_name = 'max_concurrent_raids'
        ) THEN
            ALTER TABLE dispatcher_state RENAME COLUMN max_concurrent_runs TO max_concurrent_raids;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_session_messages' AND column_name = 'run_id'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_session_messages' AND column_name = 'raid_id'
        ) THEN
            ALTER TABLE run_session_messages RENAME COLUMN run_id TO raid_id;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'run_session_messages'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'raid_session_messages'
        ) THEN
            ALTER TABLE run_session_messages RENAME TO raid_session_messages;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_confidence_events' AND column_name = 'run_id'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_confidence_events' AND column_name = 'raid_id'
        ) THEN
            ALTER TABLE run_confidence_events RENAME COLUMN run_id TO raid_id;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'run_confidence_events'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'raid_confidence_events'
        ) THEN
            ALTER TABLE run_confidence_events RENAME TO raid_confidence_events;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_progress' AND column_name = 'run_id'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_progress' AND column_name = 'raid_id'
        ) THEN
            ALTER TABLE run_progress RENAME COLUMN run_id TO raid_id;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'run_progress'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'raid_progress'
        ) THEN
            ALTER TABLE run_progress RENAME TO raid_progress;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'session_messages' AND column_name = 'run_id'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'session_messages' AND column_name = 'raid_id'
        ) THEN
            ALTER TABLE session_messages RENAME COLUMN run_id TO raid_id;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'confidence_events' AND column_name = 'run_id'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'confidence_events' AND column_name = 'raid_id'
        ) THEN
            ALTER TABLE confidence_events RENAME COLUMN run_id TO raid_id;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'runs'
        ) AND NOT EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'raids'
        ) THEN
            ALTER TABLE runs RENAME TO raids;
        END IF;
    END $$;

  000025_legacy_schema_compat.up.sql: |
    -- Forward-compatibility migration for existing legacy Ting databases.
    -- Fresh installs already create run-era tables; this heals older or partially
    -- migrated databases before later DDL expects run-era names everywhere.

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'integration_connections'
        ) THEN
            UPDATE integration_connections
            SET adapter = 'ting' || substr(adapter, 4)
            WHERE adapter LIKE concat(chr(116), chr(121), chr(114), '.%');
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'raids'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.tables
                WHERE table_schema = 'public' AND table_name = 'runs'
            ) THEN
                ALTER TABLE raids RENAME TO runs;
            ELSE
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS pr_url TEXT;
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS pr_id TEXT;
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS reason TEXT;
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS description TEXT NOT NULL DEFAULT '';
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS acceptance_criteria TEXT[] NOT NULL DEFAULT '{}';
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS reviewer_session_id TEXT;
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS review_round INTEGER NOT NULL DEFAULT 0;
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS identifier TEXT NOT NULL DEFAULT '';
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS url TEXT NOT NULL DEFAULT '';
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS depends_on TEXT[] DEFAULT '{}';
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS structured_outcome JSONB;
                ALTER TABLE runs ADD COLUMN IF NOT EXISTS outcome_event_type TEXT;

                INSERT INTO runs (
                    id,
                    phase_id,
                    tracker_id,
                    name,
                    declared_files,
                    estimate_hours,
                    status,
                    confidence,
                    session_id,
                    branch,
                    chronicle_summary,
                    retry_count,
                    created_at,
                    updated_at,
                    pr_url,
                    pr_id,
                    reason,
                    description,
                    acceptance_criteria,
                    reviewer_session_id,
                    review_round,
                    identifier,
                    url,
                    depends_on,
                    structured_outcome,
                    outcome_event_type
                )
                SELECT
                    id,
                    phase_id,
                    tracker_id,
                    name,
                    declared_files,
                    estimate_hours,
                    status,
                    confidence,
                    session_id,
                    branch,
                    chronicle_summary,
                    retry_count,
                    created_at,
                    updated_at,
                    pr_url,
                    pr_id,
                    reason,
                    description,
                    acceptance_criteria,
                    reviewer_session_id,
                    review_round,
                    identifier,
                    url,
                    depends_on,
                    structured_outcome,
                    outcome_event_type
                FROM raids
                ON CONFLICT (id) DO NOTHING;

                DROP TABLE raids;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'confidence_events' AND column_name = 'raid_id'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.columns
                WHERE table_schema = 'public' AND table_name = 'confidence_events' AND column_name = 'run_id'
            ) THEN
                ALTER TABLE confidence_events RENAME COLUMN raid_id TO run_id;
            ELSE
                UPDATE confidence_events SET run_id = COALESCE(run_id, raid_id) WHERE raid_id IS NOT NULL;
                ALTER TABLE confidence_events DROP COLUMN raid_id;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'session_messages' AND column_name = 'raid_id'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.columns
                WHERE table_schema = 'public' AND table_name = 'session_messages' AND column_name = 'run_id'
            ) THEN
                ALTER TABLE session_messages RENAME COLUMN raid_id TO run_id;
            ELSE
                UPDATE session_messages SET run_id = COALESCE(run_id, raid_id) WHERE raid_id IS NOT NULL;
                ALTER TABLE session_messages DROP COLUMN raid_id;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'raid_progress'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.tables
                WHERE table_schema = 'public' AND table_name = 'run_progress'
            ) THEN
                ALTER TABLE raid_progress RENAME TO run_progress;
            ELSE
                ALTER TABLE run_progress ADD COLUMN IF NOT EXISTS reviewer_session_id TEXT;
                ALTER TABLE run_progress ADD COLUMN IF NOT EXISTS review_round INTEGER NOT NULL DEFAULT 0;
                ALTER TABLE run_progress ADD COLUMN IF NOT EXISTS depends_on TEXT[] DEFAULT '{}';

                INSERT INTO run_progress (
                    tracker_id,
                    run_id,
                    owner_id,
                    phase_tracker_id,
                    saga_tracker_id,
                    status,
                    session_id,
                    confidence,
                    pr_url,
                    pr_id,
                    retry_count,
                    reason,
                    chronicle_summary,
                    updated_at,
                    reviewer_session_id,
                    review_round,
                    depends_on
                )
                SELECT
                    tracker_id,
                    raid_id,
                    owner_id,
                    phase_tracker_id,
                    saga_tracker_id,
                    status,
                    session_id,
                    confidence,
                    pr_url,
                    pr_id,
                    retry_count,
                    reason,
                    chronicle_summary,
                    updated_at,
                    reviewer_session_id,
                    review_round,
                    depends_on
                FROM raid_progress
                ON CONFLICT (tracker_id) DO NOTHING;

                DROP TABLE raid_progress;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_progress' AND column_name = 'raid_id'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.columns
                WHERE table_schema = 'public' AND table_name = 'run_progress' AND column_name = 'run_id'
            ) THEN
                ALTER TABLE run_progress RENAME COLUMN raid_id TO run_id;
            ELSE
                UPDATE run_progress SET run_id = COALESCE(run_id, raid_id) WHERE raid_id IS NOT NULL;
                ALTER TABLE run_progress DROP COLUMN raid_id;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'raid_confidence_events'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.tables
                WHERE table_schema = 'public' AND table_name = 'run_confidence_events'
            ) THEN
                ALTER TABLE raid_confidence_events RENAME TO run_confidence_events;
            ELSE
                INSERT INTO run_confidence_events (
                    id,
                    run_id,
                    tracker_id,
                    event_type,
                    delta,
                    score_after,
                    created_at
                )
                SELECT
                    id,
                    raid_id,
                    tracker_id,
                    event_type,
                    delta,
                    score_after,
                    created_at
                FROM raid_confidence_events
                ON CONFLICT (id) DO NOTHING;

                DROP TABLE raid_confidence_events;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_confidence_events' AND column_name = 'raid_id'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.columns
                WHERE table_schema = 'public' AND table_name = 'run_confidence_events' AND column_name = 'run_id'
            ) THEN
                ALTER TABLE run_confidence_events RENAME COLUMN raid_id TO run_id;
            ELSE
                UPDATE run_confidence_events SET run_id = COALESCE(run_id, raid_id) WHERE raid_id IS NOT NULL;
                ALTER TABLE run_confidence_events DROP COLUMN raid_id;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'raid_session_messages'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.tables
                WHERE table_schema = 'public' AND table_name = 'run_session_messages'
            ) THEN
                ALTER TABLE raid_session_messages RENAME TO run_session_messages;
            ELSE
                INSERT INTO run_session_messages (
                    id,
                    run_id,
                    tracker_id,
                    session_id,
                    content,
                    sender,
                    created_at
                )
                SELECT
                    id,
                    raid_id,
                    tracker_id,
                    session_id,
                    content,
                    sender,
                    created_at
                FROM raid_session_messages
                ON CONFLICT (id) DO NOTHING;

                DROP TABLE raid_session_messages;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'run_session_messages' AND column_name = 'raid_id'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.columns
                WHERE table_schema = 'public' AND table_name = 'run_session_messages' AND column_name = 'run_id'
            ) THEN
                ALTER TABLE run_session_messages RENAME COLUMN raid_id TO run_id;
            ELSE
                UPDATE run_session_messages SET run_id = COALESCE(run_id, raid_id) WHERE raid_id IS NOT NULL;
                ALTER TABLE run_session_messages DROP COLUMN raid_id;
            END IF;
        END IF;
    END $$;

    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'dispatcher_state' AND column_name = 'max_concurrent_raids'
        ) THEN
            IF NOT EXISTS (
                SELECT 1
                FROM information_schema.columns
                WHERE table_schema = 'public' AND table_name = 'dispatcher_state' AND column_name = 'max_concurrent_runs'
            ) THEN
                ALTER TABLE dispatcher_state RENAME COLUMN max_concurrent_raids TO max_concurrent_runs;
            ELSE
                UPDATE dispatcher_state SET max_concurrent_runs = max_concurrent_raids;
                ALTER TABLE dispatcher_state DROP COLUMN max_concurrent_raids;
            END IF;
        END IF;
    END $$;

    DO $$
    DECLARE
        old_title text := concat(chr(84), chr(121), chr(114));
        old_lower text := lower(old_title);
        old_run_title text := concat(chr(82), chr(97), chr(105), chr(100));
        old_run_lower text := lower(old_run_title);
        old_executor_name text := old_run_lower || '-executor';
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'ravn_personas'
        ) THEN
            UPDATE ravn_personas
            SET
                name = replace(name, old_executor_name, 'run-executor'),
                config_json = CASE
                    WHEN config_json IS NULL THEN NULL
                    ELSE replace(
                        replace(
                            replace(
                                replace(config_json::text, old_title, 'Ting'),
                                old_lower,
                                'ting'
                            ),
                            old_run_title,
                            'Run'
                        ),
                        old_run_lower,
                        'run'
                    )::jsonb
                END,
                runtime_config_json = CASE
                    WHEN runtime_config_json IS NULL THEN NULL
                    ELSE replace(
                        replace(
                            replace(
                                replace(runtime_config_json::text, old_title, 'Ting'),
                                old_lower,
                                'ting'
                            ),
                            old_run_title,
                            'Run'
                        ),
                        old_run_lower,
                        'run'
                    )::jsonb
                END
            WHERE
                name = old_executor_name
                OR config_json::text ILIKE '%' || old_lower || '%'
                OR config_json::text ILIKE '%' || old_run_lower || '%'
                OR runtime_config_json::text ILIKE '%' || old_lower || '%'
                OR runtime_config_json::text ILIKE '%' || old_run_lower || '%';
        END IF;
    END $$;

    DO $$
    DECLARE
        old_title text := concat(chr(84), chr(121), chr(114));
        old_lower text := lower(old_title);
        old_run_title text := concat(chr(82), chr(97), chr(105), chr(100));
        old_run_lower text := lower(old_run_title);
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'workflows'
        ) THEN
            UPDATE workflows
            SET
                name = replace(replace(replace(replace(name, old_title, 'Ting'), old_lower, 'ting'), old_run_title, 'Run'), old_run_lower, 'run'),
                description = replace(replace(replace(replace(description, old_title, 'Ting'), old_lower, 'ting'), old_run_title, 'Run'), old_run_lower, 'run'),
                definition_yaml = replace(replace(replace(replace(definition_yaml, old_title, 'Ting'), old_lower, 'ting'), old_run_title, 'Run'), old_run_lower, 'run'),
                graph_json = replace(
                    replace(
                        replace(
                            replace(graph_json::text, old_title, 'Ting'),
                            old_lower,
                            'ting'
                        ),
                        old_run_title,
                        'Run'
                    ),
                    old_run_lower,
                    'run'
                )::jsonb
            WHERE
                name ILIKE '%' || old_lower || '%'
                OR description ILIKE '%' || old_lower || '%'
                OR definition_yaml ILIKE '%' || old_lower || '%'
                OR graph_json::text ILIKE '%' || old_lower || '%'
                OR name ILIKE '%' || old_run_lower || '%'
                OR description ILIKE '%' || old_run_lower || '%'
                OR definition_yaml ILIKE '%' || old_run_lower || '%'
                OR graph_json::text ILIKE '%' || old_run_lower || '%';
        END IF;
    END $$;

    DO $$
    DECLARE
        old_title text := concat(chr(84), chr(121), chr(114));
        old_lower text := lower(old_title);
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'chronicles'
        ) THEN
            UPDATE chronicles
            SET
                branch = replace(replace(branch, old_title, 'Ting'), old_lower, 'ting'),
                config_snapshot = CASE
                    WHEN config_snapshot IS NULL THEN NULL
                    ELSE replace(
                        replace(config_snapshot::text, old_title, 'Ting'),
                        old_lower,
                        'ting'
                    )::jsonb
                END
            WHERE
                branch ILIKE '%' || old_lower || '%'
                OR config_snapshot::text ILIKE '%' || old_lower || '%';
        END IF;
    END $$;

    ALTER INDEX IF EXISTS raids_pkey RENAME TO runs_pkey;
    ALTER INDEX IF EXISTS idx_raids_phase_id RENAME TO idx_runs_phase_id;
    ALTER INDEX IF EXISTS idx_raids_status RENAME TO idx_runs_status;
    ALTER INDEX IF EXISTS idx_raids_session_id RENAME TO idx_runs_session_id;
    ALTER INDEX IF EXISTS idx_raids_reviewer_session RENAME TO idx_runs_reviewer_session;
    ALTER INDEX IF EXISTS idx_confidence_events_raid_id RENAME TO idx_confidence_events_run_id;
    ALTER INDEX IF EXISTS idx_session_messages_raid_id RENAME TO idx_session_messages_run_id;
    ALTER INDEX IF EXISTS raid_progress_pkey RENAME TO run_progress_pkey;
    ALTER INDEX IF EXISTS idx_raid_progress_status RENAME TO idx_run_progress_status;
    ALTER INDEX IF EXISTS idx_raid_progress_session RENAME TO idx_run_progress_session;
    ALTER INDEX IF EXISTS idx_raid_progress_raid_id RENAME TO idx_run_progress_run_id;
    ALTER INDEX IF EXISTS idx_raid_progress_phase RENAME TO idx_run_progress_phase;
    ALTER INDEX IF EXISTS raid_confidence_events_pkey RENAME TO run_confidence_events_pkey;
    ALTER INDEX IF EXISTS idx_raid_confidence_events_tracker RENAME TO idx_run_confidence_events_tracker;
    ALTER INDEX IF EXISTS raid_session_messages_pkey RENAME TO run_session_messages_pkey;
    ALTER INDEX IF EXISTS idx_raid_session_messages_tracker RENAME TO idx_run_session_messages_tracker;
    ALTER INDEX IF EXISTS idx_raid_session_messages_session RENAME TO idx_run_session_messages_session;

  000026_legacy_naming_cleanup.down.sql: |
    SELECT 1;

  000026_legacy_naming_cleanup.up.sql: |
    DO $$
    DECLARE
        old_title text := concat(chr(84), chr(121), chr(114));
        old_lower text := lower(old_title);
        old_accented text := convert_from(decode('54c3bd72', 'hex'), 'UTF8');
        old_run_title text := concat(chr(82), chr(97), chr(105), chr(100));
        old_run_lower text := lower(old_run_title);
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'chronicles'
        ) THEN
            UPDATE chronicles
            SET
                branch = replace(replace(replace(replace(replace(branch, old_accented, 'Ting'), old_title, 'Ting'), old_lower, 'ting'), old_run_title, 'Run'), old_run_lower, 'run'),
                summary = CASE
                    WHEN summary IS NULL THEN NULL
                    ELSE replace(replace(replace(replace(replace(summary, old_accented, 'Ting'), old_title, 'Ting'), old_lower, 'ting'), old_run_title, 'Run'), old_run_lower, 'run')
                END,
                unfinished_work = CASE
                    WHEN unfinished_work IS NULL THEN NULL
                    ELSE replace(replace(replace(replace(replace(unfinished_work, old_accented, 'Ting'), old_title, 'Ting'), old_lower, 'ting'), old_run_title, 'Run'), old_run_lower, 'run')
                END,
                key_changes = CASE
                    WHEN key_changes IS NULL THEN NULL
                    ELSE replace(replace(replace(replace(replace(key_changes::text, old_accented, 'Ting'), old_title, 'Ting'), old_lower, 'ting'), old_run_title, 'Run'), old_run_lower, 'run')::jsonb
                END,
                config_snapshot = CASE
                    WHEN config_snapshot IS NULL THEN NULL
                    ELSE replace(replace(replace(replace(replace(config_snapshot::text, old_accented, 'Ting'), old_title, 'Ting'), old_lower, 'ting'), old_run_title, 'Run'), old_run_lower, 'run')::jsonb
                END
            WHERE
                branch LIKE '%' || old_accented || '%'
                OR branch ILIKE '%' || old_lower || '%'
                OR coalesce(summary, '') LIKE '%' || old_accented || '%'
                OR coalesce(summary, '') ILIKE '%' || old_lower || '%'
                OR coalesce(unfinished_work, '') LIKE '%' || old_accented || '%'
                OR coalesce(unfinished_work, '') ILIKE '%' || old_lower || '%'
                OR coalesce(key_changes::text, '') LIKE '%' || old_accented || '%'
                OR coalesce(key_changes::text, '') ILIKE '%' || old_lower || '%'
                OR config_snapshot::text LIKE '%' || old_accented || '%'
                OR config_snapshot::text ILIKE '%' || old_lower || '%'
                OR branch ILIKE '%' || old_run_lower || '%'
                OR coalesce(summary, '') ILIKE '%' || old_run_lower || '%'
                OR coalesce(unfinished_work, '') ILIKE '%' || old_run_lower || '%'
                OR coalesce(key_changes::text, '') ILIKE '%' || old_run_lower || '%'
                OR config_snapshot::text ILIKE '%' || old_run_lower || '%';
        END IF;
    END $$;

    DO $$
    DECLARE
        old_title text := concat(chr(84), chr(121), chr(114));
        old_lower text := lower(old_title);
        old_rune text := convert_from(decode('e19b83', 'hex'), 'UTF8');
        old_accented text := convert_from(decode('54c3bd72', 'hex'), 'UTF8');
        old_run_title text := concat(chr(82), chr(97), chr(105), chr(100));
        old_run_lower text := lower(old_run_title);
        old_pending_runs text := 'pending' || old_run_title || 's';
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'observatory_registries'
        ) THEN
            UPDATE observatory_registries
            SET
                version = GREATEST(version, 8),
                updated_at = now(),
                payload = jsonb_set(
                    jsonb_set(
                        replace(
                            replace(
                                replace(
                                    replace(
                                        replace(
                                            replace(
                                                replace(payload::text, old_rune, '✦'),
                                                old_accented,
                                                'Ting'
                                            ),
                                            old_title,
                                            'Ting'
                                        ),
                                        old_lower,
                                        'ting'
                                    ),
                                    old_pending_runs,
                                    'pendingRuns'
                                ),
                                old_run_title,
                                'Run'
                            ),
                            old_run_lower,
                            'run'
                        )::jsonb,
                        '{version}',
                        to_jsonb(8),
                        true
                    ),
                    '{updatedAt}',
                    to_jsonb(to_char(timezone('UTC', now()), 'YYYY-MM-DD"T"HH24:MI:SS"Z"')),
                    true
                )
            WHERE
                payload::text LIKE '%' || old_accented || '%'
                OR payload::text ILIKE '%' || old_lower || '%'
                OR payload::text ILIKE '%' || old_run_lower || '%'
                OR payload::text LIKE '%' || old_rune || '%';
        END IF;
    END $$;

    DO $$
    DECLARE
        old_title text := concat(chr(84), chr(121), chr(114));
        old_lower text := lower(old_title);
        old_accented text := convert_from(decode('54c3bd72', 'hex'), 'UTF8');
        old_run_title text := concat(chr(82), chr(97), chr(105), chr(100));
        old_run_lower text := lower(old_run_title);
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'sagas'
        ) THEN
            UPDATE sagas
            SET workflow_snapshot = replace(
                replace(
                    replace(
                        replace(
                            replace(workflow_snapshot::text, old_accented, 'Ting'),
                            old_title,
                            'Ting'
                        ),
                        old_lower,
                        'ting'
                    ),
                    old_run_title,
                    'Run'
                ),
                old_run_lower,
                'run'
            )::jsonb
            WHERE
                workflow_snapshot::text LIKE '%' || old_accented || '%'
                OR workflow_snapshot::text ILIKE '%' || old_lower || '%'
                OR workflow_snapshot::text ILIKE '%' || old_run_lower || '%';
        END IF;
    END $$;

    DO $$
    DECLARE
        old_title text := concat(chr(84), chr(121), chr(114));
        old_lower text := lower(old_title);
        old_accented text := convert_from(decode('54c3bd72', 'hex'), 'UTF8');
        old_run_title text := concat(chr(82), chr(97), chr(105), chr(100));
        old_run_lower text := lower(old_run_title);
    BEGIN
        IF EXISTS (
            SELECT 1
            FROM information_schema.tables
            WHERE table_schema = 'public' AND table_name = 'chronicle_events'
        ) THEN
            UPDATE chronicle_events
            SET label = replace(
                replace(
                    replace(
                        replace(
                            replace(label, old_accented, 'Ting'),
                            old_title,
                            'Ting'
                        ),
                        old_lower,
                        'ting'
                    ),
                    old_run_title,
                    'Run'
                ),
                old_run_lower,
                'run'
            )
            WHERE
                label LIKE '%' || old_accented || '%'
                OR label ILIKE '%' || old_lower || '%'
                OR label ILIKE '%' || old_run_lower || '%';
        END IF;
    END $$;

  000027_saga_instance_assignment.down.sql: |
    DROP INDEX IF EXISTS idx_sagas_instance_id;

    ALTER TABLE sagas DROP COLUMN IF EXISTS instance_id;

  000027_saga_instance_assignment.up.sql: |
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS instance_id UUID;

    CREATE INDEX IF NOT EXISTS idx_sagas_instance_id ON sagas(instance_id);

  000028_workflow_graph_only.down.sql: |
    ALTER TABLE workflows
        ADD COLUMN IF NOT EXISTS definition_yaml TEXT;

  000028_workflow_graph_only.up.sql: |
    ALTER TABLE workflows
        DROP COLUMN IF EXISTS definition_yaml;

  000029_workflow_campaigns.down.sql: |
    DROP TABLE IF EXISTS workflow_campaigns;

  000029_workflow_campaigns.up.sql: |
    -- Trackerless workflow campaign records for long-running launched workflows.

    CREATE TABLE IF NOT EXISTS workflow_campaigns (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        slug TEXT NOT NULL UNIQUE,
        name TEXT NOT NULL,
        owner_id TEXT NOT NULL,
        workflow_id UUID NOT NULL,
        workflow_version TEXT NOT NULL DEFAULT 'draft',
        workflow_name TEXT NOT NULL DEFAULT '',
        workflow_snapshot JSONB NOT NULL DEFAULT '{}'::jsonb,
        session_id TEXT NOT NULL,
        session_name TEXT NOT NULL,
        status TEXT NOT NULL DEFAULT 'pending',
        active_stage_id TEXT,
        stage_state JSONB NOT NULL DEFAULT '[]'::jsonb,
        metadata JSONB NOT NULL DEFAULT '{}'::jsonb,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        last_activity_at TIMESTAMPTZ,
        completed_at TIMESTAMPTZ,
        CONSTRAINT workflow_campaigns_status_check CHECK (
            status IN ('pending', 'running', 'blocked', 'completed', 'failed')
        )
    );

    CREATE INDEX IF NOT EXISTS idx_workflow_campaigns_owner_updated_at
        ON workflow_campaigns(owner_id, updated_at DESC);

    CREATE INDEX IF NOT EXISTS idx_workflow_campaigns_status_updated_at
        ON workflow_campaigns(status, updated_at DESC);

    CREATE INDEX IF NOT EXISTS idx_workflow_campaigns_session_id
        ON workflow_campaigns(session_id);

  000030_saga_repo_refs_and_tag_targets.down.sql: |
    DROP INDEX IF EXISTS idx_sagas_target_tags;

    ALTER TABLE sagas DROP COLUMN IF EXISTS target_match;
    ALTER TABLE sagas DROP COLUMN IF EXISTS target_tags;
    ALTER TABLE sagas DROP COLUMN IF EXISTS repo_branches;

  000030_saga_repo_refs_and_tag_targets.up.sql: |
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS repo_branches JSONB NOT NULL DEFAULT '{}'::jsonb;
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS target_tags TEXT[] NOT NULL DEFAULT '{}';
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS target_match TEXT NOT NULL DEFAULT 'all';

    CREATE INDEX IF NOT EXISTS idx_sagas_target_tags ON sagas USING GIN (target_tags);

  000031_workflow_campaign_connection.down.sql: |
    ALTER TABLE workflow_campaigns DROP COLUMN IF EXISTS connection_id;

  000031_workflow_campaign_connection.up.sql: |
    -- Persist which Volundr connection a campaign's session was launched on.
    -- Read paths (status projector, A2A pending questions/gates/artifacts/cancel)
    -- must target the same Volundr instance the session lives on; resolving the
    -- owner's primary connection breaks for sessions launched on a non-default
    -- cluster.
    ALTER TABLE workflow_campaigns ADD COLUMN IF NOT EXISTS connection_id TEXT;

  000032_a2a_push_notifications.down.sql: |
    DROP TABLE IF EXISTS a2a_push_notification_configs;

  000032_a2a_push_notifications.up.sql: |
    CREATE TABLE IF NOT EXISTS a2a_push_notification_configs (
        task_id TEXT NOT NULL,
        config_id TEXT NOT NULL,
        owner_id TEXT NOT NULL,
        config_data BYTEA NOT NULL,
        pending_event JSONB,
        delivery_version TEXT,
        next_attempt_at TIMESTAMPTZ,
        attempt_count INTEGER NOT NULL DEFAULT 0,
        last_error TEXT,
        delivered_at TIMESTAMPTZ,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        PRIMARY KEY (task_id, config_id, owner_id),
        CONSTRAINT a2a_push_notification_task_fk
            FOREIGN KEY (task_id) REFERENCES workflow_campaigns(slug) ON DELETE CASCADE
    );

    CREATE INDEX IF NOT EXISTS idx_a2a_push_notification_due
        ON a2a_push_notification_configs (next_attempt_at)
        WHERE pending_event IS NOT NULL;


  000033_pat_authority.up.sql: |
    -- Existing unattributed PATs remain recorded but are not valid credentials.
    -- Rotate them through the authenticated issuance path to establish tenant/scope metadata.
    ALTER TABLE personal_access_tokens ADD COLUMN IF NOT EXISTS tenant_id TEXT NOT NULL DEFAULT '';
    ALTER TABLE personal_access_tokens ADD COLUMN IF NOT EXISTS scopes TEXT[];
    ALTER TABLE personal_access_tokens ADD COLUMN IF NOT EXISTS expires_at TIMESTAMPTZ;
    CREATE INDEX IF NOT EXISTS idx_pats_tenant_owner ON personal_access_tokens(tenant_id, owner_id);


  000033_pat_authority.down.sql: |
    -- Revoke scoped credentials before older code can treat them as unrestricted.
    -- Preserve the audit record; token_hash no longer matches any presented JWT.
    DO $$
    BEGIN
        IF EXISTS (
            SELECT 1 FROM information_schema.columns
            WHERE table_schema = current_schema()
              AND table_name = 'personal_access_tokens' AND column_name = 'scopes'
        ) THEN
            UPDATE personal_access_tokens
            SET token_hash = 'revoked:' || token_hash
            WHERE scopes IS NOT NULL AND token_hash NOT LIKE 'revoked:%';
        END IF;
    END $$;
    DROP INDEX IF EXISTS idx_pats_tenant_owner;
    ALTER TABLE personal_access_tokens DROP COLUMN IF EXISTS scopes;
    ALTER TABLE personal_access_tokens DROP COLUMN IF EXISTS tenant_id;


  000034_resource_tenants.up.sql: |
    -- Existing records have no authoritative tenant attribution. Do not infer it
    -- from a request. They remain inaccessible under Cedar until audited/backfilled.
    ALTER TABLE sagas ADD COLUMN IF NOT EXISTS tenant_id TEXT NOT NULL DEFAULT '';
    ALTER TABLE workflows ADD COLUMN IF NOT EXISTS tenant_id TEXT NOT NULL DEFAULT '';
    ALTER TABLE workflow_campaigns ADD COLUMN IF NOT EXISTS tenant_id TEXT NOT NULL DEFAULT '';
    CREATE INDEX IF NOT EXISTS idx_sagas_tenant_owner ON sagas(tenant_id, owner_id);
    CREATE INDEX IF NOT EXISTS idx_workflows_tenant_owner ON workflows(tenant_id, owner_id);
    CREATE INDEX IF NOT EXISTS idx_workflow_campaigns_tenant_owner ON workflow_campaigns(tenant_id, owner_id);


  000034_resource_tenants.down.sql: |
    DROP INDEX IF EXISTS idx_workflow_campaigns_tenant_owner;
    DROP INDEX IF EXISTS idx_workflows_tenant_owner;
    DROP INDEX IF EXISTS idx_sagas_tenant_owner;
    ALTER TABLE workflow_campaigns DROP COLUMN IF EXISTS tenant_id;
    ALTER TABLE workflows DROP COLUMN IF EXISTS tenant_id;
    ALTER TABLE sagas DROP COLUMN IF EXISTS tenant_id;


  000035_tracker_connections.up.sql: |
    -- Bind imported sagas to a specific integration connection. Provider type is
    -- descriptive and is not sufficient when an owner has multiple Jira/Linear accounts.
    ALTER TABLE sagas
        ADD COLUMN IF NOT EXISTS tracker_connection_id TEXT NOT NULL DEFAULT '';

    CREATE INDEX IF NOT EXISTS idx_sagas_tracker_connection
        ON sagas (owner_id, tracker_connection_id, tracker_id);

    ALTER TABLE run_progress
        ADD COLUMN IF NOT EXISTS tracker_connection_id TEXT NOT NULL DEFAULT '';
    ALTER TABLE run_confidence_events
        ADD COLUMN IF NOT EXISTS tracker_connection_id TEXT NOT NULL DEFAULT '';
    ALTER TABLE run_session_messages
        ADD COLUMN IF NOT EXISTS tracker_connection_id TEXT NOT NULL DEFAULT '';

    ALTER TABLE run_progress DROP CONSTRAINT IF EXISTS run_progress_pkey;
    ALTER TABLE run_progress
        ADD CONSTRAINT run_progress_pkey PRIMARY KEY (tracker_connection_id, tracker_id);

    CREATE INDEX IF NOT EXISTS idx_run_progress_connection_status
        ON run_progress (tracker_connection_id, status);
    CREATE INDEX IF NOT EXISTS idx_run_progress_connection_session
        ON run_progress (tracker_connection_id, session_id)
        WHERE session_id IS NOT NULL;
    CREATE INDEX IF NOT EXISTS idx_run_confidence_events_connection_tracker
        ON run_confidence_events (tracker_connection_id, tracker_id);
    CREATE INDEX IF NOT EXISTS idx_run_session_messages_connection_tracker
        ON run_session_messages (tracker_connection_id, tracker_id);


  000035_tracker_connections.down.sql: |
    DROP INDEX IF EXISTS idx_run_session_messages_connection_tracker;
    DROP INDEX IF EXISTS idx_run_confidence_events_connection_tracker;
    DROP INDEX IF EXISTS idx_run_progress_connection_session;
    DROP INDEX IF EXISTS idx_run_progress_connection_status;
    ALTER TABLE run_progress DROP CONSTRAINT IF EXISTS run_progress_pkey;
    ALTER TABLE run_progress ADD CONSTRAINT run_progress_pkey PRIMARY KEY (tracker_id);
    ALTER TABLE run_session_messages DROP COLUMN IF EXISTS tracker_connection_id;
    ALTER TABLE run_confidence_events DROP COLUMN IF EXISTS tracker_connection_id;
    ALTER TABLE run_progress DROP COLUMN IF EXISTS tracker_connection_id;
    DROP INDEX IF EXISTS idx_sagas_tracker_connection;
    ALTER TABLE sagas DROP COLUMN IF EXISTS tracker_connection_id;


  000036_workflow_portable_contract.up.sql: |
    ALTER TABLE workflows
        ADD COLUMN IF NOT EXISTS persona_dependencies_json JSONB NOT NULL DEFAULT '{}'::jsonb;
    ALTER TABLE workflows
        ADD COLUMN IF NOT EXISTS persona_definitions_json JSONB NOT NULL DEFAULT '{}'::jsonb;
    ALTER TABLE workflows
        ADD COLUMN IF NOT EXISTS requirements_json JSONB NOT NULL DEFAULT '[]'::jsonb;


  000036_workflow_portable_contract.down.sql: |
    ALTER TABLE workflows DROP COLUMN IF EXISTS requirements_json;
    ALTER TABLE workflows DROP COLUMN IF EXISTS persona_definitions_json;
    ALTER TABLE workflows DROP COLUMN IF EXISTS persona_dependencies_json;


  000037_workflow_executions.up.sql: |
    -- Durable parent/child ledger for developer-delivery workflow expansion.

    CREATE TABLE IF NOT EXISTS workflow_executions (
        id UUID PRIMARY KEY,
        owner_id TEXT NOT NULL,
        tenant_id TEXT NOT NULL DEFAULT '',
        name TEXT NOT NULL,
        prompt TEXT NOT NULL,
        workflow_id UUID NOT NULL,
        workflow_revision TEXT NOT NULL,
        workflow_digest TEXT NOT NULL,
        repository TEXT NOT NULL,
        base_ref TEXT NOT NULL,
        base_sha TEXT NOT NULL,
        parent_session_id TEXT NOT NULL,
        parent_node_id TEXT NOT NULL,
        connection_id TEXT NOT NULL DEFAULT '',
        state TEXT NOT NULL,
        current_generation INTEGER NOT NULL DEFAULT 0 CHECK (current_generation >= 0),
        total_budget BIGINT NOT NULL CHECK (total_budget > 0),
        reserved_budget BIGINT NOT NULL DEFAULT 0 CHECK (reserved_budget >= 0),
        spent_budget BIGINT NOT NULL DEFAULT 0 CHECK (spent_budget >= 0),
        deadline TIMESTAMPTZ NOT NULL,
        suspension_reason TEXT NOT NULL DEFAULT '',
        cancel_requested BOOLEAN NOT NULL DEFAULT FALSE,
        policy JSONB NOT NULL,
        revision BIGINT NOT NULL DEFAULT 1 CHECK (revision > 0),
        launch_key TEXT NOT NULL DEFAULT '',
        launch_digest TEXT NOT NULL DEFAULT '',
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        CONSTRAINT workflow_executions_state_check CHECK (
            state IN ('pending', 'running', 'waiting', 'blocked', 'canceling',
                      'canceled', 'completed', 'failed')
        ),
        CONSTRAINT workflow_executions_budget_check CHECK (
            reserved_budget + spent_budget <= total_budget
        )
    );

    CREATE INDEX IF NOT EXISTS idx_workflow_executions_owner_updated
        ON workflow_executions (owner_id, updated_at DESC, id DESC);
    CREATE INDEX IF NOT EXISTS idx_workflow_executions_active
        ON workflow_executions (state, deadline)
        WHERE state IN ('pending', 'running', 'waiting', 'blocked', 'canceling');
    CREATE UNIQUE INDEX IF NOT EXISTS idx_workflow_executions_launch_key
        ON workflow_executions (owner_id, tenant_id, launch_key)
        WHERE launch_key <> '';

    CREATE TABLE IF NOT EXISTS workflow_execution_generations (
        execution_id UUID NOT NULL REFERENCES workflow_executions(id) ON DELETE CASCADE,
        generation INTEGER NOT NULL CHECK (generation > 0),
        sealed BOOLEAN NOT NULL DEFAULT FALSE,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        sealed_at TIMESTAMPTZ,
        PRIMARY KEY (execution_id, generation)
    );

    CREATE TABLE IF NOT EXISTS workflow_execution_children (
        id UUID PRIMARY KEY,
        execution_id UUID NOT NULL REFERENCES workflow_executions(id) ON DELETE CASCADE,
        generation INTEGER NOT NULL CHECK (generation > 0),
        child_key TEXT NOT NULL,
        attempt INTEGER NOT NULL CHECK (attempt > 0),
        state TEXT NOT NULL,
        dependencies TEXT[] NOT NULL DEFAULT '{}',
        requirement_ids TEXT[] NOT NULL,
        objective TEXT NOT NULL,
        template_id UUID NOT NULL,
        template_revision TEXT NOT NULL,
        template_digest TEXT NOT NULL,
        plan_digest TEXT NOT NULL,
        input_digest TEXT NOT NULL,
        input JSONB NOT NULL,
        repository TEXT NOT NULL,
        base_sha TEXT NOT NULL,
        budget_units BIGINT NOT NULL CHECK (budget_units > 0),
        deadline TIMESTAMPTZ NOT NULL,
        agent_id TEXT NOT NULL,
        skill_id TEXT NOT NULL,
        intent_id UUID NOT NULL UNIQUE,
        message_id TEXT NOT NULL UNIQUE,
        task_id TEXT NOT NULL DEFAULT '',
        context_id TEXT NOT NULL DEFAULT '',
        result JSONB,
        artifacts JSONB NOT NULL DEFAULT '[]'::jsonb,
        workspace JSONB,
        evidence_report JSONB,
        evidence_validated_at TIMESTAMPTZ,
        failure_kind TEXT,
        error TEXT NOT NULL DEFAULT '',
        lease_owner TEXT NOT NULL DEFAULT '',
        lease_token UUID,
        fencing_generation BIGINT NOT NULL DEFAULT 0 CHECK (fencing_generation >= 0),
        lease_expires_at TIMESTAMPTZ,
        remote_observed_at TIMESTAMPTZ,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        UNIQUE (execution_id, generation, child_key, attempt),
        FOREIGN KEY (execution_id, generation)
            REFERENCES workflow_execution_generations(execution_id, generation) ON DELETE CASCADE,
        CONSTRAINT workflow_children_state_check CHECK (
            state IN ('reserved', 'launching', 'submitted', 'running', 'waiting',
                      'blocked', 'canceling', 'canceled', 'completed', 'failed', 'superseded')
        )
    );

    CREATE INDEX IF NOT EXISTS idx_workflow_children_execution
        ON workflow_execution_children (execution_id, generation, child_key, attempt);
    CREATE INDEX IF NOT EXISTS idx_workflow_children_launch
        ON workflow_execution_children (state, lease_expires_at, deadline)
        WHERE state IN ('reserved', 'launching');
    CREATE INDEX IF NOT EXISTS idx_workflow_children_task
        ON workflow_execution_children (agent_id, task_id)
        WHERE task_id <> '';

    CREATE TABLE IF NOT EXISTS workflow_child_events (
        child_id UUID NOT NULL REFERENCES workflow_execution_children(id) ON DELETE CASCADE,
        event_id TEXT NOT NULL,
        observed_at TIMESTAMPTZ NOT NULL,
        state TEXT NOT NULL,
        payload JSONB NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        PRIMARY KEY (child_id, event_id)
    );

    CREATE TABLE IF NOT EXISTS workflow_child_messages (
        id UUID PRIMARY KEY,
        child_id UUID NOT NULL REFERENCES workflow_execution_children(id) ON DELETE CASCADE,
        message_id TEXT NOT NULL UNIQUE,
        answer TEXT NOT NULL,
        metadata JSONB NOT NULL,
        state TEXT NOT NULL DEFAULT 'reserved',
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        delivered_at TIMESTAMPTZ,
        CONSTRAINT workflow_child_messages_state_check CHECK (
            state IN ('reserved', 'delivered')
        )
    );

    -- Receiver-side launch reservation.  It exists before a runtime session and
    -- therefore closes the old launch-before-campaign crash/duplicate window.
    CREATE TABLE IF NOT EXISTS a2a_launch_reservations (
        id UUID PRIMARY KEY,
        owner_id TEXT NOT NULL,
        tenant_id TEXT NOT NULL DEFAULT '',
        message_id TEXT NOT NULL,
        request_digest TEXT NOT NULL,
        workflow_id UUID NOT NULL,
        task_id TEXT NOT NULL UNIQUE,
        campaign_id UUID NOT NULL,
        session_id TEXT,
        state TEXT NOT NULL DEFAULT 'reserved',
        error TEXT NOT NULL DEFAULT '',
        lease_owner TEXT NOT NULL DEFAULT '',
        lease_token UUID,
        fencing_generation BIGINT NOT NULL DEFAULT 0,
        lease_expires_at TIMESTAMPTZ,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        UNIQUE (owner_id, tenant_id, message_id),
        CONSTRAINT a2a_launch_reservations_state_check CHECK (
            state IN ('reserved', 'launching', 'launched', 'failed')
        )
    );

    CREATE INDEX IF NOT EXISTS idx_a2a_launch_reservations_reconcile
        ON a2a_launch_reservations (state, lease_expires_at)
        WHERE state IN ('reserved', 'launching');

  000037_workflow_executions.down.sql: |
    DROP TABLE IF EXISTS a2a_launch_reservations;
    DROP TABLE IF EXISTS workflow_child_messages;
    DROP TABLE IF EXISTS workflow_child_events;
    DROP TABLE IF EXISTS workflow_execution_children;
    DROP TABLE IF EXISTS workflow_execution_generations;
    DROP TABLE IF EXISTS workflow_executions;

  000038_workflow_dependencies.up.sql: |
    ALTER TABLE workflows
        ADD COLUMN IF NOT EXISTS schema_version INTEGER NOT NULL DEFAULT 1;
    ALTER TABLE workflows
        ADD COLUMN IF NOT EXISTS workflow_dependencies_json JSONB NOT NULL DEFAULT '{}'::jsonb;
    ALTER TABLE workflows
        ADD COLUMN IF NOT EXISTS workflow_definitions_json JSONB NOT NULL DEFAULT '{}'::jsonb;

  000038_workflow_dependencies.down.sql: |
    ALTER TABLE workflows DROP COLUMN IF EXISTS workflow_definitions_json;
    ALTER TABLE workflows DROP COLUMN IF EXISTS workflow_dependencies_json;
    ALTER TABLE workflows DROP COLUMN IF EXISTS schema_version;

  000039_delivery_completion.up.sql: |
    ALTER TABLE workflow_executions ADD COLUMN IF NOT EXISTS merge_receipt JSONB;
    ALTER TABLE workflow_executions ADD COLUMN IF NOT EXISTS completed_at TIMESTAMPTZ;
    ALTER TABLE workflow_executions ADD COLUMN IF NOT EXISTS workflow_snapshot JSONB NOT NULL DEFAULT '{}'::jsonb;

  000039_delivery_completion.down.sql: |
    ALTER TABLE workflow_executions DROP COLUMN IF EXISTS workflow_snapshot;
    ALTER TABLE workflow_executions DROP COLUMN IF EXISTS completed_at;
    ALTER TABLE workflow_executions DROP COLUMN IF EXISTS merge_receipt;

  000040_delivery_integration_reviews.up.sql: |
    ALTER TABLE workflow_executions
        ADD COLUMN IF NOT EXISTS integration_receipts JSONB NOT NULL DEFAULT '[]'::jsonb,
        ADD COLUMN IF NOT EXISTS integration_allocation JSONB,
        ADD COLUMN IF NOT EXISTS integration_candidate JSONB,
        ADD COLUMN IF NOT EXISTS integration_review_receipt JSONB,
        ADD COLUMN IF NOT EXISTS integration_review_event_id TEXT NOT NULL DEFAULT '';

    CREATE INDEX IF NOT EXISTS idx_workflow_executions_parent_session
        ON workflow_executions (owner_id, parent_session_id)
        WHERE state NOT IN ('canceled', 'completed', 'failed');

  000040_delivery_integration_reviews.down.sql: |
    DROP INDEX IF EXISTS idx_workflow_executions_parent_session;

    ALTER TABLE workflow_executions
        DROP COLUMN IF EXISTS integration_review_event_id,
        DROP COLUMN IF EXISTS integration_review_receipt,
        DROP COLUMN IF EXISTS integration_candidate,
        DROP COLUMN IF EXISTS integration_allocation,
        DROP COLUMN IF EXISTS integration_receipts;

  000041_workflow_blocker_notifications.up.sql: |
    ALTER TABLE workflow_executions
        ADD COLUMN IF NOT EXISTS blocker_revision BIGINT NOT NULL DEFAULT 0,
        ADD COLUMN IF NOT EXISTS blocker_notified_revision BIGINT NOT NULL DEFAULT 0;

    ALTER TABLE workflow_execution_children
        ADD COLUMN IF NOT EXISTS last_polled_at TIMESTAMPTZ;

    UPDATE workflow_executions execution
    SET blocker_revision = 1
    WHERE blocker_revision = 0
      AND execution.state NOT IN ('canceled', 'completed', 'failed')
      AND EXISTS (
          SELECT 1
          FROM workflow_execution_children child
          WHERE child.execution_id = execution.id
            AND child.state IN ('blocked', 'failed')
      );

  000041_workflow_blocker_notifications.down.sql: |
    ALTER TABLE workflow_execution_children
        DROP COLUMN IF EXISTS last_polled_at;

    ALTER TABLE workflow_executions
        DROP COLUMN IF EXISTS blocker_notified_revision,
        DROP COLUMN IF EXISTS blocker_revision;

  000042_workflow_child_pending_input.up.sql: |
    ALTER TABLE workflow_execution_children
        ADD COLUMN IF NOT EXISTS pending_questions JSONB NOT NULL DEFAULT '[]'::jsonb,
        ADD COLUMN IF NOT EXISTS pending_gates JSONB NOT NULL DEFAULT '[]'::jsonb;

  000042_workflow_child_pending_input.down.sql: |
    ALTER TABLE workflow_execution_children
        DROP COLUMN IF EXISTS pending_gates,
        DROP COLUMN IF EXISTS pending_questions;

  000043_workflow_waits.up.sql: |
    CREATE TABLE IF NOT EXISTS workflow_waits (
        id UUID PRIMARY KEY,
        execution_id UUID NOT NULL REFERENCES workflow_executions(id) ON DELETE CASCADE,
        execution_generation INTEGER NOT NULL CHECK (execution_generation >= 0),
        execution_revision INTEGER NOT NULL CHECK (execution_revision > 0),
        candidate_digest TEXT NOT NULL CHECK (candidate_digest ~ '^[a-f0-9]{64}$'),
        mode TEXT NOT NULL CHECK (mode IN ('checks', 'merge')),
        request_digest TEXT NOT NULL CHECK (request_digest ~ '^[a-f0-9]{64}$'),
        request JSONB NOT NULL,
        state TEXT NOT NULL DEFAULT 'pending'
            CHECK (state IN ('pending', 'ready', 'failed', 'notified')),
        observation JSONB,
        next_poll_at TIMESTAMPTZ NOT NULL,
        attempt_count INTEGER NOT NULL DEFAULT 0 CHECK (attempt_count >= 0),
        last_error TEXT NOT NULL DEFAULT '',
        lease_owner TEXT NOT NULL DEFAULT '',
        lease_token UUID,
        fencing_generation BIGINT NOT NULL DEFAULT 0 CHECK (fencing_generation >= 0),
        lease_expires_at TIMESTAMPTZ,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        notified_at TIMESTAMPTZ,
        UNIQUE (execution_id, request_digest)
    );

    CREATE UNIQUE INDEX IF NOT EXISTS idx_workflow_waits_active_generation
        ON workflow_waits (execution_id, execution_generation)
        WHERE state <> 'notified';

    CREATE INDEX IF NOT EXISTS idx_workflow_waits_due
        ON workflow_waits (state, next_poll_at, lease_expires_at)
        WHERE state IN ('pending', 'ready', 'failed');

  000043_workflow_waits.down.sql: |
    DROP TABLE IF EXISTS workflow_waits;

  000044_workflow_plan_revision.up.sql: |
    ALTER TABLE workflow_executions
        ADD COLUMN IF NOT EXISTS plan_revision TEXT NOT NULL DEFAULT '';

  000044_workflow_plan_revision.down.sql: |
    ALTER TABLE workflow_executions
        DROP COLUMN IF EXISTS plan_revision;

  000045_workflow_parent_stop_cleanup.up.sql: |
    ALTER TABLE workflow_executions
        ADD COLUMN IF NOT EXISTS parent_stop_requested_at TIMESTAMPTZ,
        ADD COLUMN IF NOT EXISTS parent_stopped_at TIMESTAMPTZ;

    CREATE INDEX IF NOT EXISTS idx_workflow_executions_parent_stop_pending
        ON workflow_executions (parent_stop_requested_at, id)
        WHERE parent_stop_requested_at IS NOT NULL AND parent_stopped_at IS NULL;

  000045_workflow_parent_stop_cleanup.down.sql: |
    DROP INDEX IF EXISTS idx_workflow_executions_parent_stop_pending;

    ALTER TABLE workflow_executions
        DROP COLUMN IF EXISTS parent_stopped_at,
        DROP COLUMN IF EXISTS parent_stop_requested_at;

  000046_workflow_reconcile_failure_tracking.up.sql: |
    -- Durable, visible failure tracking so one poisoned reconcile item cannot
    -- starve the queue: a consecutive-failure counter per child (reset on a
    -- successful poll, terminal-failed once it exceeds the configured maximum),
    -- and an attempt/error trail for the durable parent-stop cleanup loop.

    ALTER TABLE workflow_execution_children
        ADD COLUMN IF NOT EXISTS reconcile_failure_count INTEGER NOT NULL DEFAULT 0
            CHECK (reconcile_failure_count >= 0);

    ALTER TABLE workflow_executions
        ADD COLUMN IF NOT EXISTS parent_stop_attempts INTEGER NOT NULL DEFAULT 0
            CHECK (parent_stop_attempts >= 0),
        ADD COLUMN IF NOT EXISTS parent_stop_error TEXT NOT NULL DEFAULT '';

  000046_workflow_reconcile_failure_tracking.down.sql: |
    ALTER TABLE workflow_executions
        DROP COLUMN IF EXISTS parent_stop_error,
        DROP COLUMN IF EXISTS parent_stop_attempts;

    ALTER TABLE workflow_execution_children
        DROP COLUMN IF EXISTS reconcile_failure_count;

{{- end }}
